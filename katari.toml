[package]
name = "ai"
version = "0.1.10"
description = "Provider-agnostic AI tool-calling loop for Katari: one request seam (ai.infer_step) with interchangeable Gemini, OpenAI, and Anthropic providers."

[runtime]
url = "http://localhost:3000"

[dependencies]
registry = "https://raw.githubusercontent.com/katari-lang/katari-registry/main"
snapshot = "staging"
packages = []
